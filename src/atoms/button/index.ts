import Button from './Button'
import { Defaults, Props as ButtonProps, Type, TypeProps, Variant, VariantProps } from './component'

export { Defaults }
export type { ButtonProps, Type, Variant, VariantProps, TypeProps }
export default Button
